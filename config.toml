# The URL the site will be built for
base_url = "https://blog.sanhuzhen.top"

# The site title and description; used in feeds by default.
title = "sanhuzhen's blog"
description = "sanhuzhen's blog"

theme = "nivis"

# When set to "true", a feed is automatically generated.
generate_feeds = true

# The filename to use for the feed. Used as the template filename, too.
# Defaults to "atom.xml", which has a builtin template that renders an Atom 1.0 feed.
# There is also a builtin template "rss.xml" that renders an RSS 2.0 feed.
feed_filenames = ["rss.xml", "atom.xml"]

# The taxonomies to be rendered for the site and their configuration.
# Example:
#     taxonomies = [
#       {name = "tags", feed = true}, # each tag will have its own feed
#       {name = "tags", lang = "fr"}, # you can have taxonomies with the same name in multiple languages
#       {name = "categories", paginate_by = 5},  # 5 items per page for a term
#       {name = "authors"}, # Basic definition: no feed or pagination
#     ]
#
taxonomies = [{ name = "tags", paginate_by = 5 }]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# When set to "true", a search index is built from the pages and section
# content for `default_language`.
build_search_index = false

# You can put any kind of data here. The data
# will be accessible in all templates
# Example:
#     [extra]
#     author = "Famous author"
#
# author value will be available using {{ config.extra.author }} in templates
#

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_theme = "css"

github_alerts = true
bottom_footnotes = true
render_emoji = true

[markdown.highlighting]
extra_syntaxes = ["syntaxes"]


[extra]
main_section = "posts"
math_display = "mathjax"
avatar = "images/logo.png"

social_links = [
    { name = "github", url = "https://github.com/sanhuzhen" },
    { name = "email", url = "sanhuzhen@foxmail.com" },
    { name = "rss", url = "/atom.xml" },
]